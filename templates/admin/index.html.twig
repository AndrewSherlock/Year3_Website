{% extends 'base.html.twig' %}

{% block title %}{{ controller_name }}!{% endblock %}

{% block body %}

    <div style="text-align: center">
    <h1>Admin Panel</h1>

    <p>
    <a href="{{ path('admin_foods')}}">See all foods</a>
    </p>
    <p>
    <a href="{{ path('admin_users')}}">See all Users</a>
    </p>
    <p>
    <a href="{{ path('admin_approveList')}}">See suggested public foods</a>
    </p>
    <p>
    <a href="{{ path('admin_approve_review_list')}}">See suggested public reviews</a>
    <p>
    <a href="{{ path('admin_cat_list')}}">See all Categories</a>
    </p>
    </div>
{% endblock %}
