{% extends 'base.html.twig' %}

{% block title %}{{ page_name }}!{% endblock %}

{% block body %}

    <h1 style="text-align: center">Category list</h1>

    <div style="margin-left: 50px">
    <a href="{{ path('category_new')}}" >Add new category</a><br>
    <a href="{{ path('admin_home')}}" >Back to Admin panel</a>
    </div>


    <table class="table table-sm" style="margin-top: 40px">
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Category Name</th>
            <th scope="col">Category Edit</th>
            <th scope="col">Category Delete</th>
        </tr>
        <tbody>
        {% for category in categories %}
        <tr>
            <th scope="row">{{ category.id }}</th>
            <td>{{ category.category }}</a></td>
            <td><a href="{{ path('category_edit', {'id': category.id}) }}" >Update</a></td>
            <td><a href="{{ path('category_delete', {'id': category.id}) }}" >Delete</a></td>
        </tr>
        {% endfor %}
        </tbody>

    </table>
{% endblock %}
